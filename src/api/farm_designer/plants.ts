import { API } from "../api";
import * as Axios from "axios";

export namespace Plants {
    export function find(id: number) {
        // return Axios.get<any>(API.current.plantsPath + id);
    }
};
