import { ResourceIndex } from "../../../resources/interfaces";
import { LocationData } from "./interfaces";

export function formatSelectedDropdown(ri: ResourceIndex, ld: LocationData) {
  return { label: "BRB", value: 1 }
}
